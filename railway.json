{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "corepack enable && pnpm install --no-frozen-lockfile && pnpm build --filter=@market-predict/web"
  },
  "deploy": {
    "startCommand": "pnpm --filter=@market-predict/web start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
